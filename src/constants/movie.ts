export enum Category {
  MOVIE = "movie",
  TV = "tv",
  ALL = "all",
}

export enum MovieType {
  UPCOMING = "upcoming",
  POPULAR = "popular",
  TOP_RATED = "top_rated",
}

export enum TvType {
  ON_THE_AIR = "on_the_air",
  POPULAR = "popular",
  TOP_RATED = "top_rated",
}
